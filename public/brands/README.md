# Brand packs

One directory per corp. `tripwire` is the neutral default and `example` is a
complete fictional pack to copy. The full field reference is in the main
README under **Brand packs**. Packs other than those two are git-ignored here;
keep yours in a private repo and copy it in with `scripts/brands-sync.sh`.
